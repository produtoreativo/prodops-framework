# Context Capsule

Localização canônica: gerado pelo gate de readiness do `/downstream` em `prodops/exec/cards/<card-slug>/context.md`
(path `cards` no manifest). Capsules são geradas, não escritas à mão — regenerar via
`/downstream`, nunca editar diretamente.

---

# Card: <slug>

(generated by /downstream readiness — do not edit by hand)

Date: <YYYY-MM-DD>

## OBC

Path: `prodops/artifacts/business/obcs/<capability>.md`

Success criteria (verbatim — the 3–6 measurable lines from the OBC):

- <criterion 1>
- <criterion 2>
- <criterion 3>

## BDD

Path: `prodops/artifacts/business/bdd/<capability>.feature`

Scenarios (numbered, one line each):

1. <scenario one-liner>
2. <scenario one-liner>

## Risks

Only entries relevant to this card, one line + ID each
(source: `prodops/journeys/assessment/risks.md`):

- `<RISK-ID>` — <one-line description and mitigation status>

## Reliability

SLOs, timeouts, and idempotency requirements for this card
(source: `prodops/journeys/assessment/reliability-plans/`):

- <SLO / timeout / idempotency requirement>

## Contract

Endpoint(s) and request/response essentials:

- `<METHOD> <path>` — <request essentials → response essentials>

## Commands

Preencher com todos os gates aplicáveis lidos de `prodops/exec/manifest.yaml`
(`gates`). Não copiar uma lista fixa para este template.

## Git flow

- Status: `not started` until `/hack start`
- Intended base, when already known: `<base or pending>`

## Open questions

Anything marked pending. The agent must NOT resolve these silently — stop and report.

- <pending question, or "None">
